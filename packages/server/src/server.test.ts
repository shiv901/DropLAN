import { describe, it } from 'vitest';

describe('Server', () => {
  it('should be importable', async () => {
    // Integration tests for startServer require a real port — skipped in unit test suite.
    // See packages/server/src/server.ts for the implementation.
  });
});
